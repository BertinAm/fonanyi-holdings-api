[pytest]
DJANGO_SETTINGS_MODULE = config.settings_test
python_files = test_*.py
# Tests here are function-based; without this pytest tries to collect the
# Testimonial model as a test class.
python_classes =
addopts = -q --no-header
filterwarnings =
    ignore::UserWarning:django.core.handlers.base
